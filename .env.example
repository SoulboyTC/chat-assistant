# 复制这个文件为 .env，然后填入自己的配置。
# .env 已被 .gitignore 排除，不会上传。

# 1) 火山方舟（豆包大模型）—— 在 https://console.volcengine.com/ark 创建
# 填模型 ID 或推理接入点 ep-xxxxxxxx；没开通会报 ModelNotOpen
# 模型名可以写多个候选（逗号隔开），程序会自动挑第一个已开通的用
ARK_API_KEY=
ARK_MODEL=doubao-seed-2-1-turbo-260628

# 接口地址一般不用改
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 2) 任意 OpenAI 兼容接口（OpenRouter / Vercel / 自建中转）
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
# OPENAI_MODEL=openai/gpt-4o-mini

# 3) Jev（TypeSafe System One）—— 判断类任务又快又便宜，只判断不生成
# 2.0 已留好插槽：填了它就走「Jev 判断 + 生成模型起草」两步
# ⚠️ 目前官方暂停了新用户注册（2026-09-23 起），暂时拿不到 key
# 等能注册了，把下面两行取消注释并填入即可
# TYPESAFE_API_KEY=
# TYPESAFE_MODEL=jev-latest
# TYPESAFE_API_URL=https://api.typesafe.ai/v1/systemone
